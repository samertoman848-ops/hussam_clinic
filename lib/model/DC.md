# شرح مفصل لمجلد: lib\model

هذا المجلد يحتوي على ملفات برمجية بلغة Dart لإدارة وظائف العيادة.


## ملف: `DatesModel.dart`

**الوصف العام:** هذا ملف "نموذج بيانات" (Model) يُستخدم لتعريف بنية البيانات التي يتعامل معها النظام (Data Structure).

**الكلاسات المعرفة (Classes):**
- `DateModel`

**أهم الوظائف (Key Functions):**
- `id()`
- `kind()`
- `place()`
- `dateStart()`
- `dateEnd()`
- `note()`
- `doctorId()`
- `doctorName()`
- `costumerId()`
- `costumerName()`

---

## ملف: `PictureModel.dart`

**الوصف العام:** هذا ملف "نموذج بيانات" (Model) يُستخدم لتعريف بنية البيانات التي يتعامل معها النظام (Data Structure).

**الكلاسات المعرفة (Classes):**
- `PictureModel`

**أهم الوظائف (Key Functions):**
- `pictureId()`
- `patient_pic_patientId()`
- `pictureLocation()`

---

## ملف: `RoomModel.dart`

**الوصف العام:** هذا ملف "نموذج بيانات" (Model) يُستخدم لتعريف بنية البيانات التي يتعامل معها النظام (Data Structure).

**الكلاسات المعرفة (Classes):**
- `RoomModel`

---

