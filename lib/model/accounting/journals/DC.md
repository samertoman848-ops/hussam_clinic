# شرح مفصل لمجلد: lib\model\accounting\journals

هذا المجلد يحتوي على ملفات برمجية بلغة Dart لإدارة وظائف العيادة.


## ملف: `IndexModel.dart`

**الوصف العام:** هذا ملف "نموذج بيانات" (Model) يُستخدم لتعريف بنية البيانات التي يتعامل معها النظام (Data Structure).

**الكلاسات المعرفة (Classes):**
- `IndexModel`

**أهم الوظائف (Key Functions):**
- `ini_balance()`
- `last_tran_date()`
- `buying_currency()`
- `buying_price()`
- `selling_currency()`
- `selling_price()`
- `max_qty()`
- `min_qty()`
- `balance()`
- `unit_name()`

---

## ملف: `JournalsDetailModel.dart`

**الوصف العام:** هذا ملف "نموذج بيانات" (Model) يُستخدم لتعريف بنية البيانات التي يتعامل معها النظام (Data Structure).

**الكلاسات المعرفة (Classes):**
- `JournalsDetailModel`

**أهم الوظائف (Key Functions):**
- `acc_amount()`
- `rate()`
- `currency()`
- `description()`
- `credit()`
- `debit()`
- `account_name()`
- `account_id()`
- `id()`

---

## ملف: `journalsModel.dart`

**الوصف العام:** هذا ملف "نموذج بيانات" (Model) يُستخدم لتعريف بنية البيانات التي يتعامل معها النظام (Data Structure).

**الكلاسات المعرفة (Classes):**
- `JournalsModel`

**أهم الوظائف (Key Functions):**
- `id()`
- `date()`
- `discription()`
- `rate()`
- `currency()`
- `amount()`
- `time()`

---

