# شرح مفصل لمجلد: lib\model\accounting\invoices

هذا المجلد يحتوي على ملفات برمجية بلغة Dart لإدارة وظائف العيادة.


## ملف: `InvoicesDetailModel.dart`

**الوصف العام:** هذا ملف "نموذج بيانات" (Model) يُستخدم لتعريف بنية البيانات التي يتعامل معها النظام (Data Structure).

**الكلاسات المعرفة (Classes):**
- `InvoicesDetailModel`

**أهم الوظائف (Key Functions):**
- `id()`
- `invoices_id()`
- `item_no()`
- `item_name()`
- `unit_name()`
- `unit_qty()`
- `unit_price()`
- `net_price()`

---

## ملف: `InvoicesModel.dart`

**الوصف العام:** هذا ملف "نموذج بيانات" (Model) يُستخدم لتعريف بنية البيانات التي يتعامل معها النظام (Data Structure).

**الكلاسات المعرفة (Classes):**
- `InvoicesModel`

**أهم الوظائف (Key Functions):**
- `type()`
- `discription()`
- `jornal()`
- `remaining()`
- `accountingTo_no()`
- `payment_currency()`
- `payment()`
- `currency()`
- `amount_all()`
- `disscount()`

---

