# شرح مفصل لمجلد: lib\widgets

هذا المجلد يحتوي على ملفات برمجية بلغة Dart لإدارة وظائف العيادة.


## ملف: `animated_card.dart`

**الوصف العام:** هذا ملف "عنصر واجهة" (Widget) وهو جزء صغير من الواجهة يمكن إعادة استخدامه في شاشات مختلفة.

**الكلاسات المعرفة (Classes):**
- `AnimatedCard`
- `_AnimatedCardState`

**أهم الوظائف (Key Functions):**
- `createState()`
- `initState()`
- `dispose()`

---

## ملف: `app_drawer.dart`

**الوصف العام:** هذا ملف "عنصر واجهة" (Widget) وهو جزء صغير من الواجهة يمكن إعادة استخدامه في شاشات مختلفة.

**الكلاسات المعرفة (Classes):**
- `AppDrawer`

---

## ملف: `dental_chart.dart`

**الوصف العام:** هذا ملف "عنصر واجهة" (Widget) وهو جزء صغير من الواجهة يمكن إعادة استخدامه في شاشات مختلفة.

**الكلاسات المعرفة (Classes):**
- `DentalChartWidget`
- `_DentalChartWidgetState`
- `ToothPainter`

**أهم الوظائف (Key Functions):**
- `createState()`
- `initState()`
- `didUpdateWidget()`
- `dispose()`
- `debugPrint()`
- `paint()`
- `shouldRepaint()`

---

## ملف: `page_transition.dart`

**الوصف العام:** هذا ملف "عنصر واجهة" (Widget) وهو جزء صغير من الواجهة يمكن إعادة استخدامه في شاشات مختلفة.

**الكلاسات المعرفة (Classes):**
- `PageTransition`

---

## ملف: `TimePickerWithBookedHours.dart`

**الوصف العام:** هذا ملف "عنصر واجهة" (Widget) وهو جزء صغير من الواجهة يمكن إعادة استخدامه في شاشات مختلفة.

**الكلاسات المعرفة (Classes):**
- `TimePickerWithBookedHours`

---

