# شرح مفصل لمجلد: lib\pages\employment

هذا المجلد يحتوي على ملفات برمجية بلغة Dart لإدارة وظائف العيادة.


## ملف: `PageAddEmployee.dart`

**الوصف العام:** هذا ملف "واجهة مستخدم" (UI Page) يمثل شاشة كاملة تظهر للمستخدم لإدارة وظيفة معينة.

**الكلاسات المعرفة (Classes):**
- `PageAddEmployee`
- `_PageAddEmployeeState`

**أهم الوظائف (Key Functions):**
- `createState()`

---

## ملف: `PageEditEmployee.dart`

**الوصف العام:** هذا ملف "واجهة مستخدم" (UI Page) يمثل شاشة كاملة تظهر للمستخدم لإدارة وظيفة معينة.

**الكلاسات المعرفة (Classes):**
- `PageEditEmployee`
- `_PageEditEmployeeState`

**أهم الوظائف (Key Functions):**
- `createState()`
- `initState()`

---

## ملف: `PageEmployees.dart`

**الوصف العام:** هذا ملف "واجهة مستخدم" (UI Page) يمثل شاشة كاملة تظهر للمستخدم لإدارة وظيفة معينة.

**الكلاسات المعرفة (Classes):**
- `PageEmployees`
- `_PageEmployeesState`

**أهم الوظائف (Key Functions):**
- `createState()`
- `initState()`

---

