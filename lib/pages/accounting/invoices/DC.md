# شرح مفصل لمجلد: lib\pages\accounting\invoices

هذا المجلد يحتوي على ملفات برمجية بلغة Dart لإدارة وظائف العيادة.


## ملف: `expenseInvoices.dart`

**الوصف العام:** هذا ملف "واجهة مستخدم" (UI Page) يمثل شاشة كاملة تظهر للمستخدم لإدارة وظيفة معينة.

**الكلاسات المعرفة (Classes):**
- `ExpenseInvoices`
- `ExpenseInvoicesState`

**أهم الوظائف (Key Functions):**
- `createState()`
- `dispose()`
- `initState()`
- `copyExternalDB()`
- `showDialog()`
- `onConfirm()`
- `tabledata()`

---

## ملف: `expenseInvoicesReview.dart`

**الوصف العام:** هذا ملف "واجهة مستخدم" (UI Page) يمثل شاشة كاملة تظهر للمستخدم لإدارة وظيفة معينة.

**الكلاسات المعرفة (Classes):**
- `ExpenseInvoicesReview`
- `ExpenseInvoicesReviewState`

**أهم الوظائف (Key Functions):**
- `createState()`
- `initState()`
- `datatable()`

---

## ملف: `saleInvoicesReview.dart`

**الوصف العام:** هذا ملف "واجهة مستخدم" (UI Page) يمثل شاشة كاملة تظهر للمستخدم لإدارة وظيفة معينة.

**الكلاسات المعرفة (Classes):**
- `SaleInvoicesReview`
- `SaleInvoicesReviewState`

**أهم الوظائف (Key Functions):**
- `createState()`
- `print()`
- `initState()`
- `datatable()`

---

## ملف: `SalesInvoices.dart`

**الوصف العام:** هذا ملف "واجهة مستخدم" (UI Page) يمثل شاشة كاملة تظهر للمستخدم لإدارة وظيفة معينة.

**الكلاسات المعرفة (Classes):**
- `SalesInvoices`
- `SalesInvoicesState`

**أهم الوظائف (Key Functions):**
- `createState()`
- `dispose()`
- `initState()`
- `print()`
- `showDialog()`
- `copyExternalDB()`
- `tabledata()`

---

## ملف: `SalesInvoicesOptions.dart`

**الوصف العام:** هذا ملف "واجهة مستخدم" (UI Page) يمثل شاشة كاملة تظهر للمستخدم لإدارة وظيفة معينة.

**الكلاسات المعرفة (Classes):**
- `SalesInvoicesOptions`

**أهم الوظائف (Key Functions):**
- `print()`

---

