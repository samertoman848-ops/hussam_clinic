# شرح مفصل لمجلد: lib\pages\accounting\vouchers

هذا المجلد يحتوي على ملفات برمجية بلغة Dart لإدارة وظائف العيادة.


## ملف: `receiptVoucher.dart`

**الوصف العام:** هذا ملف "واجهة مستخدم" (UI Page) يمثل شاشة كاملة تظهر للمستخدم لإدارة وظيفة معينة.

**الكلاسات المعرفة (Classes):**
- `ReceiptVoucherPage`
- `_ReceiptVoucherPageState`

**أهم الوظائف (Key Functions):**
- `createState()`
- `dispose()`
- `initState()`
- `showDialog()`
- `onConfirm()`

---

## ملف: `receiptVouchersReview.dart`

**الوصف العام:** هذا ملف "واجهة مستخدم" (UI Page) يمثل شاشة كاملة تظهر للمستخدم لإدارة وظيفة معينة.

**الكلاسات المعرفة (Classes):**
- `ReceiptVouchersReview`
- `_ReceiptVouchersReviewState`

**أهم الوظائف (Key Functions):**
- `createState()`
- `initState()`

---

