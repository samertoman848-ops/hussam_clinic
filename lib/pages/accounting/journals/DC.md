# شرح مفصل لمجلد: lib\pages\accounting\journals

هذا المجلد يحتوي على ملفات برمجية بلغة Dart لإدارة وظائف العيادة.


## ملف: `journalsPage.dart`

**الوصف العام:** هذا ملف "واجهة مستخدم" (UI Page) يمثل شاشة كاملة تظهر للمستخدم لإدارة وظيفة معينة.

**الكلاسات المعرفة (Classes):**
- `JournalsPage`
- `JournalsPageState`

**أهم الوظائف (Key Functions):**
- `createState()`
- `dispose()`
- `initState()`
- `firstRow()`
- `secondRow()`
- `thirdRow()`
- `copyExternalDB()`
- `tabledata()`

---

## ملف: `journalsReview.dart`

**الوصف العام:** هذا ملف "واجهة مستخدم" (UI Page) يمثل شاشة كاملة تظهر للمستخدم لإدارة وظيفة معينة.

**الكلاسات المعرفة (Classes):**
- `JournalsReview`
- `JournalsReviewState`

**أهم الوظائف (Key Functions):**
- `createState()`
- `initState()`
- `datatable()`
- `columns()`

---

