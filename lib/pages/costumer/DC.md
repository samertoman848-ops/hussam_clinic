# شرح مفصل لمجلد: lib\pages\costumer

هذا المجلد يحتوي على ملفات برمجية بلغة Dart لإدارة وظائف العيادة.


## ملف: `PageAddCostumers.dart`

**الوصف العام:** هذا ملف "واجهة مستخدم" (UI Page) يمثل شاشة كاملة تظهر للمستخدم لإدارة وظيفة معينة.

**الكلاسات المعرفة (Classes):**
- `PageAddCostumers`
- `_PageAddCostumersState`
- `_PatienHealthtModel`

**أهم الوظائف (Key Functions):**
- `createState()`
- `addCostumers()`
- `addCostumerhealth()`
- `copyExternalDB()`
- `initState()`
- `selecedDoctorList()`
- `debugPrint()`
- `inputDecoration()`
- `inputDecorationNoIcon()`
- `pickDate()`

---

## ملف: `PageCostumers.dart`

**الوصف العام:** هذا ملف "واجهة مستخدم" (UI Page) يمثل شاشة كاملة تظهر للمستخدم لإدارة وظيفة معينة.

**الكلاسات المعرفة (Classes):**
- `PageCostumers`
- `_PageCostumersState`

**أهم الوظائف (Key Functions):**
- `createState()`
- `initState()`

---

## ملف: `PageEditCostumers.dart`

**الوصف العام:** هذا ملف "واجهة مستخدم" (UI Page) يمثل شاشة كاملة تظهر للمستخدم لإدارة وظيفة معينة.

**الكلاسات المعرفة (Classes):**
- `PageEditCostumers`
- `_PageEditCostumersState`

**أهم الوظائف (Key Functions):**
- `createState()`
- `debugPrint()`
- `editCostumers()`
- `editeCostumerhealth()`
- `initState()`
- `selecedDoctorList()`
- `intValues()`
- `showDialog()`
- `inputDecoration()`
- `inputDecorationNoIcon()`

---

