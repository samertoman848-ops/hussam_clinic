# شرح مفصل لمجلد: lib\db\accounting\invoices

هذا المجلد يحتوي على ملفات برمجية بلغة Dart لإدارة وظائف العيادة.


## ملف: `dbinvoicedetails.dart`

**الوصف العام:** هذا ملف "قاعدة بيانات" (Database Helper) مسؤول عن عمليات الإدخال والحذف والتعديل والجلب من SQLite.

**الكلاسات المعرفة (Classes):**
- `DbInvoicesDetails`

**أهم الوظائف (Key Functions):**
- `addInvoicesDetails()`
- `searchInvoicesDetails()`
- `getAllInvoicesDetails()`
- `addInvoicesDetails2()`
- `deleteInvoicesDetailsByInvoiceNo()`
- `print()`
- `deleteInvoicesDetailById()`

---

## ملف: `dbinvoices.dart`

**الوصف العام:** هذا ملف "قاعدة بيانات" (Database Helper) مسؤول عن عمليات الإدخال والحذف والتعديل والجلب من SQLite.

**الكلاسات المعرفة (Classes):**
- `DbInvoices`

**أهم الوظائف (Key Functions):**
- `allInvioces()`
- `expenseInvioces()`
- `getAllInvoices()`
- `addInvoices()`
- `addInvoices2()`
- `updateInvoices()`
- `deleteInvoice()`

---

