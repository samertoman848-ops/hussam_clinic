# شرح مفصل لمجلد: lib\db\accounting

هذا المجلد يحتوي على ملفات برمجية بلغة Dart لإدارة وظائف العيادة.


## ملف: `dbindex.dart`

**الوصف العام:** هذا ملف "قاعدة بيانات" (Database Helper) مسؤول عن عمليات الإدخال والحذف والتعديل والجلب من SQLite.

**الكلاسات المعرفة (Classes):**
- `DbIndex`

**أهم الوظائف (Key Functions):**
- `allAccountingIndexes()`
- `adddate()`

---

## ملف: `dbtree.dart`

**الوصف العام:** هذا ملف "قاعدة بيانات" (Database Helper) مسؤول عن عمليات الإدخال والحذف والتعديل والجلب من SQLite.

**الكلاسات المعرفة (Classes):**
- `DbTree`

**أهم الوظائف (Key Functions):**
- `allAccountingTree()`
- `allAccountingTreeGrouping()`
- `allEmployeeAccounting()`
- `allPaitentsAccounting()`
- `allPaitentsFromPatients()`
- `print()`
- `allSuppliersAccounting()`

---

