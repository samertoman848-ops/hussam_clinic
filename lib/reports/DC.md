# شرح مفصل لمجلد: lib\reports

هذا المجلد يحتوي على ملفات برمجية بلغة Dart لإدارة وظائف العيادة.


## ملف: `reportExpenseInvoicePDF.dart`

**الوصف العام:** تعريف المنطق البرمجي للملف.

**الكلاسات المعرفة (Classes):**
- `reportExpenseInvoicePDF`

**أهم الوظائف (Key Functions):**
- `inti()`
- `createPdfDocument()`
- `savePDFFile()`
- `buildTableExportItems()`
- `remaningMony()`
- `payedMony()`

---

## ملف: `reportSalesInvoicePDF.dart`

**الوصف العام:** تعريف المنطق البرمجي للملف.

**الكلاسات المعرفة (Classes):**
- `reportSalesInvoicePDF`

**أهم الوظائف (Key Functions):**
- `inti()`
- `createPdfDocument()`
- `savePDFFile()`
- `buildTableExportItems()`
- `remaningMony()`
- `payedMony()`

---

