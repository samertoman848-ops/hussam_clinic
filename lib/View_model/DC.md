# شرح مفصل لمجلد: lib\View_model

هذا المجلد يحتوي على ملفات برمجية بلغة Dart لإدارة وظائف العيادة.


## ملف: `ViewModelEditCostumers.dart`

**الوصف العام:** هذا ملف "نموذج بيانات" (Model) يُستخدم لتعريف بنية البيانات التي يتعامل معها النظام (Data Structure).

**الكلاسات المعرفة (Classes):**
- `ViewModelEditCostumers`

**أهم الوظائف (Key Functions):**
- `editCostumers()`
- `editeCostumerhealth()`
- `inputDecoration()`
- `inputDecorationNoIcon()`

---

## ملف: `ViewModelExpenseInvoices.dart`

**الوصف العام:** هذا ملف "نموذج بيانات" (Model) يُستخدم لتعريف بنية البيانات التي يتعامل معها النظام (Data Structure).

**الكلاسات المعرفة (Classes):**
- `ViewModelExpenseInvoices`

**أهم الوظائف (Key Functions):**
- `selecedId()`
- `selecedIndexId()`
- `checkValues2()`
- `checkValues()`
- `pickDate()`
- `picktime()`
- `inputDecorationNoIcon()`
- `inputDecoration()`

---

## ملف: `ViewModelGlobal.dart`

**الوصف العام:** هذا ملف "نموذج بيانات" (Model) يُستخدم لتعريف بنية البيانات التي يتعامل معها النظام (Data Structure).

**الكلاسات المعرفة (Classes):**
- `ViewModelGlobal`

---

## ملف: `ViewModelJournals.dart`

**الوصف العام:** هذا ملف "نموذج بيانات" (Model) يُستخدم لتعريف بنية البيانات التي يتعامل معها النظام (Data Structure).

**الكلاسات المعرفة (Classes):**
- `ViewModelJournals`

**أهم الوظائف (Key Functions):**
- `pickDate()`
- `picktime()`
- `inputDecorationNoIcon()`
- `inputDecoration()`

---

## ملف: `ViewModelJournalsDetailsReview.dart`

**الوصف العام:** هذا ملف "نموذج بيانات" (Model) يُستخدم لتعريف بنية البيانات التي يتعامل معها النظام (Data Structure).

**الكلاسات المعرفة (Classes):**
- `ViewModelJournalsDetailsReview`

**أهم الوظائف (Key Functions):**
- `selecedId()`
- `selecedIndexId()`
- `checkValues2()`
- `checkValues()`
- `pickDate()`
- `picktime()`
- `inputDecorationNoIcon()`
- `inputDecoration()`

---

## ملف: `ViewModelSalesInvoices.dart`

**الوصف العام:** هذا ملف "نموذج بيانات" (Model) يُستخدم لتعريف بنية البيانات التي يتعامل معها النظام (Data Structure).

**الكلاسات المعرفة (Classes):**
- `ViewModelSalesInvoices`

**أهم الوظائف (Key Functions):**
- `selecedId()`
- `selecedIndexId()`
- `checkValues2()`
- `checkValues()`
- `pickDate()`
- `picktime()`
- `print()`
- `inputDecorationNoIcon()`
- `inputDecoration()`

---

