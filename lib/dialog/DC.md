# شرح مفصل لمجلد: lib\dialog

هذا المجلد يحتوي على ملفات برمجية بلغة Dart لإدارة وظائف العيادة.


## ملف: `dating_add_dialog.dart`

**الوصف العام:** تعريف المنطق البرمجي للملف.

**الكلاسات المعرفة (Classes):**
- `DatingAddDialog`
- `_DatingAddDialogState`
- `_DemoEvent`

**أهم الوظائف (Key Functions):**
- `createState()`
- `selecedtdatePlaceList()`
- `initState()`
- `selecedPatientList()`
- `selecedDoctorList()`
- `dispose()`
- `selecedPatientId()`
- `selecedDoctorId()`
- `textEditFisrtPage()`
- `pickDate()`

---

## ملف: `dating_add_doctors.dart`

**الوصف العام:** تعريف المنطق البرمجي للملف.

**الكلاسات المعرفة (Classes):**
- `DatingAddDoctors`
- `_DatingAddDoctorsState`
- `_DemoEvent`

**أهم الوظائف (Key Functions):**
- `createState()`
- `selecedtdatePlaceList()`
- `initState()`
- `selecedPatientList()`
- `selecedDoctorList()`
- `dispose()`
- `selecedPatientId()`
- `selecedDoctorId()`
- `textEditFisrtPage()`
- `pickDate()`

---

## ملف: `dating_edite_dialog.dart`

**الوصف العام:** تعريف المنطق البرمجي للملف.

**الكلاسات المعرفة (Classes):**
- `DatingEditeDialog`
- `_DatingEditeDialogState`
- `_DemoEvent`

**أهم الوظائف (Key Functions):**
- `createState()`
- `selecedtdatePlaceList()`
- `initState()`
- `selecedPatientList()`
- `selecedDoctorList()`
- `dispose()`
- `selecedPatientId()`
- `selecedDoctorId()`
- `textEditFisrtPage()`
- `pickDate()`

---

## ملف: `message_send.dart`

**الوصف العام:** تعريف المنطق البرمجي للملف.

**الكلاسات المعرفة (Classes):**
- `MessageSend`

---

