# شرح مفصل لمجلد: lib\services

هذا المجلد يحتوي على ملفات برمجية بلغة Dart لإدارة وظائف العيادة.


## ملف: `firebase_sync_service.dart`

**الوصف العام:** تعريف المنطق البرمجي للملف.

**الكلاسات المعرفة (Classes):**
- `FirebaseSyncService`

**أهم الوظائف (Key Functions):**
- `initialize()`
- `debugPrint()`
- `syncData()`
- `syncPatient()`
- `pushPatient()`
- `syncPatientHealth()`
- `pushPatientHealth()`
- `syncInvoice()`
- `pushInvoice()`
- `syncInvoiceDetail()`

---

## ملف: `notification_service.dart`

**الوصف العام:** تعريف المنطق البرمجي للملف.

**الكلاسات المعرفة (Classes):**
- `NotificationService`

**أهم الوظائف (Key Functions):**
- `init()`
- `startMonitoring()`
- `print()`
- `dispose()`

---

## ملف: `StorageService.dart`

**الوصف العام:** تعريف المنطق البرمجي للملف.

**الكلاسات المعرفة (Classes):**
- `StorageService`

**أهم الوظائف (Key Functions):**
- `loadConfig()`
- `print()`
- `saveConfig()`
- `moveDataTo()`

---

